<Window x:Class="StudentsBook.HomeworkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentsBook"
        mc:Ignorable="d"
        Title="HomeworkWindow" Height="450" Width="800" ResizeMode="NoResize">
    <Grid DataContext="{Binding Homeworks}">
        <ListBox ItemsSource="{Binding AllHomeworks}" SelectedItem="{Binding SelectedPickedHomework}" HorizontalAlignment="Left" Height="388" Margin="10,10,0,0" VerticalAlignment="Top" Width="300"/>
        <ListBox ItemsSource="{Binding SettedHomeworks}" SelectedItem="{Binding SelectedSettedHomework}" HorizontalAlignment="Left" Height="388" Margin="438,10,0,0" VerticalAlignment="Top" Width="300"/>
        <Button Content="Добавить" Command="{Binding AddCommand}" CommandParameter="{Binding SelectedPickedHomework}" HorizontalAlignment="Left" Margin="315,171,0,0" VerticalAlignment="Top" Width="118"/>
        <Button Content="Убрать" Command="{Binding RemoveCommand}" CommandParameter="{Binding SelectedSettedHomework}" HorizontalAlignment="Left" Margin="315,196,0,0" VerticalAlignment="Top" Width="118"/>

    </Grid>
</Window>
